---
name: Rules
route: /Rules
sidebar: true
---
# Rules (规则组件)

事项规则展示

**何时使用**

- 存在于有规则（需规则校验）的事项下面。
- 规则组件

**样式与代码展示**

```jsx
<Rules dataSource={this.state.gzData} />
```

**API**

| 参数       | 说明   | 类型  | 默认值 |
| ---------- | ------ | ----- | ------ |
| dataSource | 数据源 | array | -      |
